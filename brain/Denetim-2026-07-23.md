# Kod Denetim Raporu — 2026-07-23

> Fable inceleme promptlarıyla yapılan salt-okunur denetim. Hiçbir dosya değiştirilmedi.
> Düzeltmeler Üveys ile TEK TEK karar verilerek yapılacak — toplu düzeltme YAPMA.

## 1. MANTIK HATALARI

🔴 **`app.py:257-258` + `recorder.py:45-49` — auto-stop yarış durumu.** 120 sn limitinde `_end()` callback thread'inden çağrılıyor; kullanıcı tuşu bırakıp tekrar basarsa `_record_started_at` kilit DIŞINDA atandığı için (`app.py:252`) süre hesabı yanlış thread'in başlangıç zamanını okuyabilir, `MIN_RECORD_SEC` filtresi yanlış karar verir.

🔴 **`app.py:266-267` — begin/end sıralaması atomik değil.** Çift basış/bounce senaryosunda ilk `_end` henüz `stop_recording()` çağırmadıysa yeni kaydın frame'lerini alıp yeni kaydı sıfırlar → ikinci dikte sessizce kaybolur.

🟡 **`app.py:106-135` — `_poll_mic` ile `_begin` yarışı.** `_mic_ok` ataması kilitsiz; stream ölüyken kayıt "başlar", hiç frame gelmez, kullanıcı "Ses yok" görür (veri kaybı değil, kafa karıştıran sessiz hata).

🟡 **`app.py:271` — `MIN_RECORD_SEC` pre-roll'u hesaba katmıyor.** 0.4 sn basış atılıyor ama elde ~0.9 sn ses var. Filtre `len(audio) / SAMPLE_RATE` üzerinden yapılmalı.

🟡 **`injector.py:37-57` — odak/pencere kontrolü yok.** Odak Finder'daysa Cmd-V dosya yapıştırır; odak yoksa metin kaybolur. Accessibility izni yoksa `CGEventPost` sessizce hiçbir şey yapmaz, uyarı çıkmaz.

🟡 **`injector.py:44` — pano yedeği sadece düz metin.** Panodaki resim/dosya dikte sonrası kaybolur.

🟡 **`recorder.py:45-49` — `MAX_RECORD_SEC` aşımında kayıt fiilen durmuyor**, sadece callback tetikleniyor; `_end` gecikirse bellek büyümeye devam eder.

🟢 `transcriber.py:34` — indirme kesilirse yarım `.indiriliyor` dosyası temizlenmiyor (disk çöpü). `app.py:51` — açılışta `ollama_available()` ana thread'de, 1.5 sn'ye kadar donma.

## 2. ÇAKIŞAN / TEKRARLANAN KOD

🟡 `config.py:43` — `FILLER_TOKENS` içinde `"ee"` iki kez (ölü).
🟡 Sessizlik kontrolü 3 ayrı yerde çelişen eşiklerle (`app.py:289` RMS kapısı, `transcriber.py:77-79` peak normalizasyon, `cleaner.py:22-28` halüsinasyon temizliği). `config.py:29-31` yorumu kendi içinde çelişkili: boş oda eşiği geçip whisper'a gidiyor.
🟡 `cleaner.py:82-89` (`ollama_serving`) ile `:137-147` (`ollama_available`) aynı endpoint'e aynı istek; `clean()` HER diktede `ollama_available()` ile 1.5 sn'ye kadar bekliyor (`cleaner.py:195`).
🟢 `app.py:90-102` ile `:315-321` aynı üçlü durum mantığı iki kez. `config.py:53` "teşekkür ederim" yorumu ("segment bazlı") kodla uyuşmuyor — kontrol tüm metin bazlı.

## 3. KOD YAPISI

🟢 Genel yapı iyi (en uzun dosya app.py 333 satır, tarihli bug yorumları değerli).
🟡 `app.py:167-208` (`_toggle_llm`) 42 satır, 3 senaryo tek fonksiyonda; `rumps.alert` ana thread'i bloklar.
🟡 `app.py:279-313` (`_process`) — `import numpy` fonksiyon içinde, modül seviyesine alınmalı.
🟢 `recorder.py:62-63` — `sd._terminate()/_initialize()` private API (requirements 0.5.5'e sabit, risk düşük). Üç bool bayrak yerine tek durum enum'u yarış pencerelerini azaltır.

## 4. RİSKLER

🔴 **`injector.py` — Accessibility izni yoksa sessiz başarısızlık + metin kaybı.** Transkript üretilmiş, pano geri yüklenmiş, Cmd-V hiçbir yere gitmemiş → konuşma yok olur. Çözüm: `AXIsProcessTrusted()` kontrolü + başarısızlıkta eski panoyu GERİ YÜKLEME, metni panoda bırak + durum çubuğunda "Yapıştırılamadı — metin panoda".

🟡 **`run.py:18` + `app.py:295,302` — dikte metinleri herkese okunur `/tmp/wispherklon.log`'a düz metin yazılıyor.** "Ses makineden çıkmaz" gizlilik vaadiyle çelişiyor; log rotasyonsuz büyüyor. `~/Library/Logs/Wispherklon/` (0600) + içerik yerine uzunluk logla.

🟡 `transcriber.py:34-45` — model indirmede bütünlük kontrolü yok; sunucu erken keserse eksik dosya rename edilir → whisper'da kriptik hata.
🟡 `cleaner.py:103-108` — uygulama kapansa da ollama çalışmaya devam eder (bilinçli ama görünür olmalı).
🟡 **`recorder.py:116-120` — `stop_stream`'de `finally` yok:** stop/close istisna fırlatırsa `self._stream = None` çalışmaz, hot-plug izleyici yeniden açmayı denemez.
🟢 Input Monitoring izni reddedilirse listener sessizce olay almaz, teşhis sunulmaz.

## 5. TAVSİYELER (öneri + kazanç + iş yükü)

1. 🔴 Enjeksiyon güvenlik ağı (AXIsProcessTrusted + metni panoda bırak) → dikte asla kaybolmaz → **küçük**
2. 🔴 begin/end/auto-stop atomikliği (`_record_started_at` kilide + yeniden basış koruması) → sessiz kayıt kaybı biter → **küçük-orta**
3. 🟡 Log gizliliği (~/Library/Logs + içerik loglama opsiyonel) → **küçük**
4. 🟡 `ollama_available()` sonucunu 30-60 sn cache'le → her diktedeki 1.5 sn gecikme biter → **küçük**
5. 🟡 `stop_stream`'e finally + model indirme boyut doğrulama + yarım dosya temizliği → **küçük**
6. 🟢 Durum makinesi refaktörü (3 bool → enum) → **orta**

## 6. TOKEN VERİMLİLİĞİ

🟢 .gitignore eksiksiz (.venv/dist/build/__pycache__/*.bin hepsi hariç). Kaynak ~955 satır — incelemede 8 dosya doğrudan paralel okunmalı, keşif turu gereksiz. brain/ dosyaları sadece "neden bu tasarım" sorusunda açılmalı.

## ÖNCE ŞUNLARI YAP

1. **Injector güvenlik ağı** — Accessibility kontrolü + başarısızlıkta metin panoda kalsın (~15 satır, en büyük veri kaybı riski).
2. **`_record_started_at`'ı kilide al** + auto-stop sonrası yeniden basış koruması (`app.py:247-277`).
3. **`recorder.stop_stream`'e `finally: self._stream = None`** (`recorder.py:116-120`).
4. **Dikte metnini /tmp loguna yazmayı kaldır** veya logu kullanıcı dizinine taşı.
5. **`clean()` içindeki `ollama_available()` çağrısına cache** ekle.
